import numpy as np
import random

class Human:

    def __init__(self):

        age_in_yrs = np.random.randint(0, 70)
        self.age = age_in_yrs

        infection = [infected, not_infected]
        infection_statuss = random.choice(infection)
        self.infection_status = infection_statuss


        self.health_status =

        x_coordinate = np.random.randint(0, 100)
        y_coordinate = np.random.randint(0, 100)

        self.coordinate = (x_coordinate, y_coordinate)


        
